import { Meta, Canvas } from "@storybook/blocks";
import * as stories from "./Markdown.stories";

<Meta of={stories} />

# 📝 Markdown

Компонент **Markdown** позволяет безопасно рендерить Markdown-разметку с поддержкой HTML и кастомных стилей.
Используется `react-markdown` с `rehype-raw`, `remarkGfm` и `rehype-sanitize` (настроенный), что защищает от XSS-атак.

## 🔹 Возможности

- Поддержка **Markdown** и **GFM** (GitHub Flavored Markdown)
- Рендеринг HTML-контента (опционально)
- Кастомные классы для стилизации
- Поддержка ссылок, изображений, таблиц и списков
- Безопасный рендеринг с возможностью настройки

## 🚀 Использование

### 1. Базовый пример

```tsx
export const Page = () => {
  return (
    <Markdown>
      {`# Заголовок 1\n\nТекст **жирный**, *курсив*, ~~зачеркнутый~~.`}
    </Markdown>
  );
};
```

<Canvas of={stories.Default} />

---

### 2. Расширенный Markdown

```tsx
export const Page = () => {
  return (
    <Markdown>
      {`# 📌 Пример сложного Markdown
| Имя      | Возраст | Город     |
|----------|--------|-----------|
| Иван     | 25     | Москва    |
| Ольга    | 30     | Санкт-Петербург |

\n\n\`\`\`tsx
const hello = "Hello, world!";
console.log(hello);
\`\`\`

![Логотип](/public/promo-card.png)
> _Цитата_: "Markdown — это просто и удобно!"
    `}
    </Markdown>
  );
};
```

<Canvas of={stories.AdvancedMarkdown} />

---

### 3. Поддержка HTML

```tsx
export const Page = () => {
  return (
    <Markdown>
      {`<h1 style='font-size: 40px'>Заголовок H1</h1><p style='color: red'>Красный текст</p>`}
    </Markdown>
  );
};
```

<Canvas of={stories.WithHTML} />

---

### 4. Кастомные стили

```tsx
export const Page = () => {
  return (
    <Markdown className="text-purple-400">
      {`### 🌟 Кастомные стили
- Это список
- Стили применяются через className
- Можно настроить разную типографику  `}
    </Markdown>
  );
};
```

<Canvas of={stories.CustomStyled} />
